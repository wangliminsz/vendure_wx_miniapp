page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background-color: #ffffff;
}

/* 页面标题 */
.page-title {
  text-align: center;
  font-size: 34rpx;
  font-weight: 500;
  margin: 32rpx 0;
}

.section {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
  padding: 0 32rpx;
  box-sizing: border-box;
  margin-top: 20px;
}

/* 头像行 + 昵称行 统一对齐规则（核心修复） */
.myavatar-wrapper, .nickname-wrapper {
  display: flex;
  /* justify-content: space-between; */
  align-items: center;
  width: 100%;
  padding: 32rpx 0;
  border-bottom: 1rpx solid #E5E6EB;
}

.nickname-label {
  font-size: 32rpx;
  color: #333;
  font-weight: 500;
  line-height: 1;
}

/* 头像按钮严格居中 */
.avatar_button {
  padding: 0;
  border: none;
  background: transparent;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}
.avatar_button::after {
  border: none;
}
.avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 60rpx;
  background-color: #f5f5f5;
}

/* 昵称输入框严格右对齐，和头像位置匹配 */
.nickname-input {
  font-size: 32rpx;
  color: #333;
  text-align: right;
  background: transparent;
  padding: 0;
  line-height: 1;
}
.nickname-placeholder {
  color: #999 !important;
  font-size: 30rpx;
}

/* 协议勾选居中 */
.agreement-container {
  margin-top: 50px;
  /* margin-bottom: 50rpx; */
  width: 100%;
  display: flex;
  justify-content: center;
}
.checkbox {
  display: flex;
  align-items: center;
  font-size: 28rpx;
  color: #666;
}
.link {
  color: #007AFF;
  margin: 0 4rpx;
}

/* 提交按钮居中 */
.loginbtn {
  width: 100%;
  /* max-width: 520rpx; */
  /* height: 96rpx; */
  background-color: #165DFF;
  color: #ffffff;
  /* font-size: 32rpx; */
  /* font-weight: bold; */
  /* border: none; */
  /* border-radius: 12rpx; */
  margin-top: 50px;
}
.loginbtn::after {
  border: none;
}
.loginbtn[disabled] {
  background-color: #cccccc;
  color: #999;
}

/* 底部协议链接居中 */
/* .user_container {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  margin-top: 48rpx;
} */

.user_container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  /* padding: 200rpx 0; */
  box-sizing: border-box;
  color: #000 !important
} 

.link {
  color: #007AFF;
  display: inline;
}

/* .user_container .link {
  font-size: 28rpx;
  color: #007AFF;
  margin-bottom: 16rpx;
} */